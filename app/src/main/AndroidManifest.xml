<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".EverythingIsFine"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EverythingIsFineKata">

        <activity
            android:name=".presentation.activity.HomeActivity"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".presentation.activity.AboutActivity"
            android:launchMode="singleTask"
            android:theme="@style/Theme.EverythingIsFineKata.NoActionBar" />

        <activity
            android:name=".presentation.activity.SearchActivity"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustNothing" />

    </application>

</manifest>